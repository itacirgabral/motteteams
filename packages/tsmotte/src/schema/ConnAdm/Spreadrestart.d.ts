/**
 * * Tipo Spreadrestart, requisita pra se reiniciar o baterista reverso
 */

export interface Spreadrestart {
  type: "spreadrestart";
  hardid: string;
  shard: string;
  url: string;
  mitochondria: string;
  cacapa: string;
}
